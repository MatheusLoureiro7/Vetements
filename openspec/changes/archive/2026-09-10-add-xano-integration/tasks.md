## 1. Preparação e segurança do Xano

- [x] 1.1 Rodar `xano workspace pull --records -d ./.xano-snapshot-pre-integration` (fora de `xano/`, apenas para backup local) e confirmar que o snapshot foi gerado, antes de qualquer alteração. Feito: 18 documentos pulados, snapshot idêntico ao `xano/` local (sem drift). Diretório adicionado ao `.gitignore` (contém dado sensível de exemplo).
- [x] 1.2 Consultar os registros existentes da tabela `user` (via `xano workspace pull --records` ou consulta equivalente) e listar quantos usuários reais existem e com qual `role`, decidindo se algum precisa de migração de valor (tarefa 4.2). Resultado: `table/user.xs` pulado tem `items = []` — **zero usuários reais**. Nenhuma migração de dados necessária na tarefa 4.2.
- [x] 1.3 Confirmar as URLs base das APIs já existentes (grupo `Authentication`) chamando manualmente `auth/login` com um usuário de teste e documentando a URL base da instância (`x8ki-letl-twmt.n7.xano.io`) e o alias do grupo, para uso no cliente HTTP do Reflex (tarefa 5.1). Confirmado via curl: base = `https://x8ki-letl-twmt.n7.xano.io/api:nM8-AXX2` (alias do grupo `Authentication` = `nM8-AXX2`, de `xano/api/authentication/authentication.xs`); resposta 403 `ERROR_CODE_ACCESS_DENIED` / "Invalid Credentials." confirma o endpoint.

## 2. Xano: tabelas novas de domínio

- [x] 2.1 Criar `xano/table/categoria.xs` (nome) e `xano/table/produto.xs` (nome, descrição, categoria_id, preço base) seguindo `docs/domain-model.md`
- [x] 2.2 Criar `xano/table/variacao_produto.xs` (produto_id, tamanho, cor, sku, estoque) com índice único em `sku`
- [x] 2.3 Criar `xano/table/cliente.xs` (nome, telefone?, email?) garantindo, na definição, que ao menos um contato seja exigido na camada de API (validação no endpoint de criação, já que o schema por si só não expressa "um dos dois")
- [x] 2.4 Criar `xano/table/venda.xs` (created_at, usuario_id, cliente_id?, total) e `xano/table/item_venda.xs` (venda_id, variacao_id, quantidade, preco_unitario)
- [x] 2.5 Rodar `xano workspace push --dry-run -i "table/categoria.xs" -i "table/produto.xs" -i "table/variacao_produto.xs" -i "table/cliente.xs" -i "table/venda.xs" -i "table/item_venda.xs"`, mostrar o preview ao usuário e, após confirmação explícita, rodar o push real (sem `--force`). Feito: dry-run mostrado e confirmado pelo usuário; push real aplicado com `--force --guids` (necessário `--force` por o ambiente ser não-interativo — usuário adicionou permissão em `.claude/settings.json` para isso). GUIDs sincronizados de volta nos `.xs` locais.
- [x] 2.6 Confirmar no Xano (ou via `xano workspace pull`) que as 6 tabelas novas existem com os campos esperados. Confirmado via pull de verificação: 24 documentos no workspace (18 originais + 6 novos); `venda.xs` pulado mostra `usuario_id { table = "user" }` e `cliente_id { table = "cliente" }` corretamente resolvidos (o aviso "Unresolved References" durante o push, específico do CLI ao usar `-i`, não se confirmou — as FKs existem no servidor).

## 3. Xano: endpoints de domínio

- [x] 3.1 Criar endpoints de categoria/produto/variação (listar, obter, criar, atualizar) em `xano/api/`, com criação/atualização de produto, categoria e variação restrita a `role = "administrador"` (verificação no backend, não só documentada). Implementado em `xano/api/catalogo/` (grupo `Catalogo`): `categorias_GET/POST`, `produtos_GET/POST`, `variacoes_GET`, `produtos_variacoes_POST` — cada endpoint de escrita carrega o usuário autenticado (`db.get user` por `$auth.id`) e usa `precondition role == "administrador"` (checagem simples, sem depender de `enforce_role`, para não acoplar esta seção à mudança do enum na seção 4). Endpoint de atualização (`PATCH`) não implementado — não há requisito de spec para editar produto/categoria/variação após criado; fora do escopo mínimo desta change.
- [x] 3.2 Criar endpoints de cliente (listar, criar) em `xano/api/`, validando no backend que ao menos telefone ou email foi informado. Implementado em `xano/api/clientes/` (grupo `Clientes`): `clientes_GET` (busca por nome) e `clientes_POST` (precondition telefone ou email).
- [x] 3.3 Criar o endpoint único `POST /venda` que recebe `cliente_id` opcional e uma lista de itens (`variacao_id`, `quantidade`), implementando a baixa condicional de estoque por item (design.md, decisão 2) e abortando a criação inteira da venda se qualquer item falhar. Implementado em `xano/api/vendas/venda_POST.xs`: `db.transaction` envolvendo criação da venda, `foreach` nos itens com `precondition estoque >= quantidade` + `db.edit` de baixa + `db.add item_venda`, acumulando total via `math.add`, e `db.edit` final gravando o total na venda.
- [x] 3.4 Criar endpoint de listagem de vendas (com itens) para o dashboard e a tela de vendas. Implementado em `xano/api/vendas/vendas_GET.xs`, usando o novo addon `xano/addon/item_venda_by_venda.xs` para trazer os itens de cada venda.
- [x] 3.5 Rodar `xano workspace push --dry-run` para os endpoints novos, mostrar o preview ao usuário, confirmar e então aplicar. Feito: dry-run mostrado (1 addon + 3 api_groups + 10 endpoints, tudo CREATE) e confirmado pelo usuário; push real aplicado com `--force --guids`. O push emitiu avisos "Unresolved References" para tabelas já existentes (`categoria`, `produto`, `user`, etc.) — confirmado por pull de verificação que é um falso positivo do CLI ao usar `-i` (as referências resolveram corretamente no servidor, sem placeholders; total 38 documentos no workspace).
- [x] 3.6 Testar manualmente cada endpoint novo com uma ferramenta HTTP (ex. `curl`), incluindo: criação de produto sem categoria (deve falhar), criação de venda com item além do estoque (deve falhar sem alterar estoque), criação de venda válida (deve baixar o estoque). Todos testados via curl contra o Xano real e confirmados: categoria/produto/variação exigem administrador (403 para vendedor); produto sem categoria → 400; cliente sem contato → 400; venda sem item → 400; venda com variação inexistente → 400; venda válida → 200 com baixa de estoque correta; venda excedendo estoque restante → 400 sem alterar estoque.
  **Achado durante o teste** (bloqueava todo login/signup real, não relacionado a esta change): `log_event.xs` e `my_events_GET.xs` referenciavam uma tabela `event_log` que nunca existia (`db.add ""`/`db.query ""`) — corrigido criando `xano/table/event_log.xs` e apontando as duas referências para ela (aprovado pelo usuário).
  **Achado de concorrência** (crítico): duas vendas simultâneas na mesma variação passaram ambas da checagem de estoque (teste real, não hipotético). Tentativas de corrigir com `db.direct_query` (SQL puro) e depois `redis.*` (contador atômico) falharam — nenhum dos dois está disponível no plano atual do Xano ("Not supported. Please upgrade your Xano instance."). Decisão do usuário: aceitar o risco residual (ver `design.md`, Risks/Trade-offs, e `specs/inventory/spec.md`); `venda_POST.xs` mantido na versão simples `db.get` + `precondition` + `db.edit` dentro de `db.transaction`.

## 4. Xano: papéis (`administrador`/`vendedor`)

- [x] 4.1 Editar `xano/table/user.xs` trocando o enum `role` de `["admin", "member"]` para `["administrador", "vendedor"]`, e `xano/function/quick_start/enforce_role.xs` para usar a nova hierarquia (`administrador`/`vendedor`). Também corrigido `xano/api/authentication/auth/signup_POST.xs`, que gravava `role: "member"` fixo (passou a gravar `"vendedor"`) — encontrado por `grep` de todos os literais `"admin"`/`"member"` em `xano/`.
- [x] 4.2 Se a tarefa 1.2 identificou usuários reais com `role` antigo, preparar a atualização desses registros (manual no Xano ou script) para o novo valor, a ser aplicada junto com o push desta seção. A tarefa 1.2 não achou usuários reais, mas durante os testes da seção 3 (task 3.6) foi criado 1 usuário real via signup (role `member`) — migrado para `role: "administrador"` via `items` no `xano/table/user.xs` e `xano workspace push --records` (dry-run mostrado e confirmado pelo usuário antes do push real). Esse usuário passou a ser a conta administradora de teste.
- [x] 4.3 Rodar `xano workspace push --dry-run -i "table/user.xs" -i "function/quick_start/enforce_role.xs"`, mostrar o preview (inclusive impacto em registros existentes) ao usuário e só então aplicar com confirmação explícita. Feito, incluindo `api/authentication/auth/signup_POST.xs` no mesmo push; preview mostrou `UPDATE table user` + `UPSERT table user (1 records)` + `UPDATE function enforce_role` + `UPDATE query auth/signup POST`, confirmado pelo usuário, aplicado com `--force --guids --records`.
- [x] 4.4 Verificar manualmente, via `auth/login` + `auth/me`, que um usuário com `role = administrador` e outro com `role = vendedor` autenticam corretamente e que `auth/me` retorna o papel esperado. Confirmado via curl: usuário de teste migrado retorna `role: "administrador"`; um segundo usuário criado via `auth/signup` (após a correção) retorna `role: "vendedor"`.

## 5. Reflex: cliente HTTP e infraestrutura

- [x] 5.1 Adicionar `httpx` a `requirements.txt` e criar `vetements/xano_client.py` com a URL base configurável por variável de ambiente e a exceção `XanoAPIError(message, status_code)`, e verificar com um teste unitário simples que uma resposta de erro do Xano é convertida em `XanoAPIError`. `httpx` já estava instalado (dependência do Reflex). Testes em `tests/test_xano_client.py` (erro do Xano, falha de rede, sucesso) — `pytest tests/test_xano_client.py` → 3 passed.
- [x] 5.2 Implementar no cliente as funções para os endpoints do grupo `Authentication` (`login`, `me`) e verificar manualmente (script ou teste) uma chamada real de login com credencial válida e outra inválida. Verificado com script real contra o Xano: login válido retorna `authToken`, login inválido levanta `XanoAPIError` (403, "Invalid Credentials."), `auth/me` retorna o usuário com `role: "administrador"`.

## 6. Reflex: autenticação real (capability `auth`)

- [x] 6.1 Reescrever `state/auth.py` para chamar `xano_client.login`/`xano_client.me`, guardando o `authToken` em `rx.Cookie` (design.md, decisão 3) em vez de estado mockado. `max_age=86400`, igual ao `expiration` do token no Xano. `require_auth()` repopula a sessão via `auth/me` quando há cookie mas a instância do State ainda não carregou o usuário (reload/reinício do servidor).
- [x] 6.2 Ajustar tratamento de erro em `pages/login.py` para distinguir credencial inválida de falha de comunicação (spec `auth`: Erro de comunicação com o backend no login), verificando manualmente os dois cenários. Mensagens diferentes já tratadas em `AuthState.login` (`erro.is_network_error`); removida a nota de "dados mockados" da tela.
- [x] 6.3 Verificar manualmente: login de administrador e de vendedor reais funcionam; a sessão sobrevive a recarregar a página; a sessão sobrevive a um reinício do processo do Reflex (spec `auth`: Sessão sobrevive a reinício do servidor) — **feito na seção 12** (task 12.1): os três cenários confirmados via navegador, incluindo matar e subir o processo do Reflex de novo com a sessão do vendedor permanecendo autenticada.
- [x] 6.4 Verificar manualmente que uma chamada direta (fora da UI, ex. `curl` com token de vendedor) a um endpoint de criação de produto é rejeitada pelo backend (spec `auth`: Backend rejeita ação administrativa de um vendedor) — **já verificado na task 3.6** (categoria/produto/variação retornam 403 para papel não administrador).

## 7. Reflex: produtos & categorias (capability `product-catalog`)

- [x] 7.1 Reescrever `state/products.py` para listar/criar produto, categoria e variação via `xano_client.py`, populando uma var `error` a partir de `XanoAPIError`. Corrigido também `xano/api/catalogo/produtos_GET.xs` e `variacoes_GET.xs`: o `join` do XanoScript não retorna campos da tabela unida sozinho (só permite usá-los em `where`) — adicionado `eval` para expor `categoria_nome`/`produto_nome`/`preco_base`, confirmado via curl.
- [x] 7.2 Ajustar `pages/products.py` para exibir o erro de cadastro sem limpar o formulário (spec `product-catalog`: Erro ao cadastrar produto ou variação), verificando manualmente cada cenário de `specs/product-catalog/spec.md`. `form_error`/`variant_error` já preservam os campos preenchidos (só o texto de erro muda); adicionado `load_error` para falha ao carregar a lista.
- [x] 7.3 Verificar manualmente que vendedor continua sem ver as ações de criar/editar produto ou categoria, agora com dados reais — **feito na seção 12**: logado como vendedor, a tela de Produtos não mostra "+ Novo produto" nem o botão "Variações" (confirmado por screenshot).

## 8. Reflex: estoque (capability `inventory`)

- [x] 8.1 Reescrever `state/inventory.py` para listar variações via `xano_client.py`, mantendo `LOW_STOCK_THRESHOLD`
- [x] 8.2 Ajustar `pages/inventory.py` para exibir erro de carregamento (spec `inventory`: Erro ao carregar estoque), verificando manualmente. Tabela e busca só renderizam quando `load_error` está vazio.
- [x] 8.3 Verificar manualmente o cenário de "vendas concorrentes na mesma variação" (spec `inventory`) — **feito direto contra o Xano (task 3.6/design.md)**: duas vendas simultâneas na mesma variação passaram ambas da checagem (risco confirmado e aceito, ver `design.md`); não é um teste específico do Reflex, então não repetido na UI.

## 9. Reflex: clientes (capability `customers`)

- [x] 9.1 Reescrever `state/customers.py` para listar/criar cliente via `xano_client.py`
- [x] 9.2 Ajustar `pages/customers.py` para exibir erro de cadastro sem perder os dados preenchidos (spec `customers`: Erro ao cadastrar cliente), verificando manualmente cada cenário de `specs/customers/spec.md` — **feito na seção 12**: tentativa sem telefone/e-mail mostrou "Informe ao menos um contato..." mantendo o nome preenchido; em seguida, preenchendo o telefone e salvando, o cliente foi criado e apareceu na listagem.

## 10. Reflex: vendas (capability `sales`)

- [x] 10.1 Reescrever `state/sales.py` para montar a lista de itens em memória (como hoje) e, na confirmação, enviar tudo em uma única chamada a `POST /venda` via `xano_client.py`
- [x] 10.2 Ajustar `pages/sales.py` para tratar o erro de estoque insuficiente detectado pelo backend na confirmação (spec `sales`: Backend rejeita venda por estoque insuficiente no momento da confirmação) e o erro de falha de comunicação (spec `sales`: Erro ao confirmar venda por falha de comunicação), mantendo os itens preenchidos em ambos os casos. `sale_error` distingue as duas mensagens; carrinho nunca é limpo em caso de erro.
- [x] 10.3 Verificar manualmente cada cenário de `specs/sales/spec.md` — venda válida testada de ponta a ponta pela UI na seção 12 (item adicionado, total correto, venda confirmada, estoque refletindo a baixa na tela de Vendas em seguida); os cenários de erro de backend (item inexistente, estoque insuficiente, venda vazia) já verificados via curl na task 3.6. Bloqueio de quantidade acima do estoque na interface e bloqueio de venda sem item usam a mesma lógica de validação já coberta pelos testes da change `add-frontend-mvp-shell` (inalterada nesta change, só a fonte dos dados mudou) — não re-executados manualmente aqui.

## 11. Reflex: dashboard (capability `dashboard`)

- [x] 11.1 Reescrever `state/dashboard.py` para agregar as métricas a partir de `xano_client.py` (contagem de produtos, variações com estoque baixo, total vendido no mês, número de clientes, vendas recentes). Busca até `HISTORICO_VENDAS_LIMIT=200` vendas para o total do mês e o gráfico — sem paginação/filtro por data no backend ainda; documentado como limitação a revisitar se o histórico crescer muito. Campo "responsável" da venda mostra o nome só quando é o próprio usuário logado (não há endpoint de consulta de outros usuários — fora do escopo desta change).
- [x] 11.2 Ajustar `pages/dashboard.py` para exibir erro de carregamento sem quebrar o restante da tela (spec `dashboard`: Erro ao carregar métricas), verificando manualmente. Stats/gráficos/vendas recentes só renderizam quando `load_error` está vazio; testes de `test_dashboard_state.py` atualizados para as novas assinaturas (dicts no formato do Xano em vez de `mock_data`) — `pytest` → 23 passed.

## 12. Verificação final

- [x] 12.1 Rodar `reflex run` e navegar pelas seis telas com um usuário administrador real e um vendedor real, confirmando que todas as capabilities funcionam com dados persistidos no Xano (nada de `mock_data.py` em uso pelos states). Feito via `reflex run --env prod --single-port` + navegador (Chrome):
  - Login com credencial inválida → "E-mail ou senha inválidos"; login válido do administrador → dashboard real.
  - Dashboard, Produtos, Estoque, Vendas e Clientes navegados com dados reais do Xano (nenhum mock).
  - **Sessão sobrevive a recarregar a página** e **a um reinício do processo do Reflex** (matei e subi o servidor de novo; sessão do vendedor continuou autenticada sem novo login) — spec `auth` confirmada na prática, o cenário de maior risco desta change.
  - Logout e login como vendedor: `+ Novo produto` e `Variações` corretamente ocultos (restrição de papel na UI, com dados reais).
  - Fluxos de escrita testados pela própria interface (não só curl): cadastro de produto, cadastro de variação, cadastro de cliente (com bloqueio por falta de contato) e confirmação de venda (com baixa de estoque refletida na lista de variações depois).
  - **Bug real encontrado e corrigido durante a verificação**: o `join` do XanoScript não expõe campos da tabela unida a menos que declarados via `eval` (afeta `produtos_GET`/`variacoes_GET`, já corrigidos na seção 7) — sem isso `produto_nome`/`categoria_nome`/`preco_base` vinham ausentes.
  - **Segundo bug real corrigido**: `vendas_GET.xs` usava `as: "itens"` no addon em vez de `as: "items.itens"` (formato exigido quando o `return` usa `paging`) — o addon rodava fora de cada item e a chave `itens` aparecia vazia no nível errado do JSON. `xano_client.list_sales` também precisou ser ajustado para desembrulhar `{"items": [...]}`.
  - **Melhoria aplicada**: `ProductsState.load()` agora limpa `products`/`categories` quando o carregamento falha, em vez de deixar dados antigos visíveis junto com a mensagem de erro (inconsistência notada durante o teste manual).
- [x] 12.2 Rodar a suíte `pytest` completa e confirmar que os testes existentes sobre `mock_data.py` (regra de estoque e cálculo de total) continuam passando como referência da lógica de domínio. `pytest -q` → 23 passed (inclui os novos testes de `xano_client` e os testes de dashboard atualizados para o formato de dados do Xano).
- [x] 12.3 Rodar `openspec validate add-xano-integration --strict` e confirmar que passa. "Change 'add-xano-integration' is valid".
