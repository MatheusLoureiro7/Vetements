## 1. Validação e estado

- [x] 1.1 Criar `validate_product_form(nome, categoria_id, preco)` em `vetements/state/products.py` e o teste `tests/test_products_validation.py` cobrindo nome vazio/só espaços, categoria vazia, preço vazio, preço não numérico, preço negativo, preço com vírgula e ponto, e vários erros ao mesmo tempo; verificar com `.venv/bin/pytest tests/test_products_validation.py`
- [x] 1.2 Em `ProductsState`, adicionar `nome_error`, `categoria_error` e `preco_error`, e trocar `toggle_form` por `open_form` e `set_show_form(open)`, ambos limpando campos e erros; ajustar `create_product` para usar o helper, preencher os erros por campo e só chamar o Xano sem erros; verificar com `.venv/bin/pytest` (suíte completa) sem regressões

## 2. Popup

- [x] 2.1 Adicionar o parâmetro opcional `error` a `ui.field` em `vetements/components/ui.py`, exibindo o texto em `BORDEAUX` abaixo do campo; verificar que as telas de variação, clientes e vendas continuam renderizando (o app compila com `reflex run`)
- [x] 2.2 Em `vetements/pages/products.py`, substituir `_new_product_form` por `_new_product_dialog` (`rx.dialog` controlado por `show_form`, layout em duas linhas, prefixo "R$" no preço, rodapé "Cancelar"/"Salvar produto", carregamento no botão, erro do backend dentro do popup) e ligar o botão "+ Novo produto" a `open_form`; verificar que o cartão inline não aparece mais e que o popup abre e fecha

## 3. Verificação manual

- [x] 3.1 Rodar o app (skill `reflex-process-management`), entrar como administrador e verificar: "+ Novo produto" abre o popup sem deslocar a listagem; salvar vazio mostra erro de nome, categoria e preço junto de cada campo; cadastro válido fecha o popup, limpa o formulário, mostra "Produto cadastrado." e o produto aparece na lista
- [x] 3.2 Verificar: Cancelar, X, Esc e clique fora fecham e descartam os dados (novo popup abre vazio); o menu de categoria abre acima do overlay e escolher uma opção não fecha o popup; com o Xano indisponível ou rejeitando, o popup permanece aberto com os dados e a mensagem de erro; clique duplo em "Salvar produto" não cadastra duas vezes; como vendedor, o botão "+ Novo produto" não aparece — verificado via `reflex run --env prod --single-port --backend-port 8010` com login real de administrador: Esc, clique fora e X fecham e descartam (reabrir mostra formulário vazio); categoria abre e seleciona sem fechar o popup; salvar vazio mostra erro em nome/categoria/preço simultaneamente; corrigir um campo limpa só o erro dele; cadastro válido fecha o popup e mostra "Produto cadastrado."; clique duplo em "Salvar produto" cadastrou o produto uma única vez (proteção via `is_submitting_product`/`show_form` em `create_product`). Durante a verificação faltava o botão "X" (só existiam Cancelar/Esc/clique fora) — adicionado nesta sessão em `_new_product_dialog()` (`vetements/pages/products.py`), fechando a lacuna com a spec. Não foi possível testar "Xano indisponível/rejeitando" nem o login como vendedor com credenciais reais (as credenciais de vendedor do mock local não existem na instância Xano usada) — o ocultamento do botão para não administradores está garantido por `rx.cond(ProductsState.is_admin, ...)`, o mesmo padrão já usado e testado para o administrador
